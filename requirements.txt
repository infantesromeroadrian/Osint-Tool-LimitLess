# Flask Web Framework
flask==2.3.3
python-dotenv==1.0.0

# Basic utilities
requests==2.31.0

# LangChain RAG Stack (Compatible versions)
langchain==0.2.16
langchain-openai==0.1.25
langchain-community==0.2.16
langchain-chroma==0.1.4

# OpenAI (Updated for compatibility)
openai==1.54.4
httpx==0.27.2

# Vector Database
chromadb==0.4.17

# Text Processing (Updated for compatibility)
tiktoken==0.8.0

# Image Processing for Metadata
Pillow==10.1.0

# Video/Audio Metadata Support
# NOTE: FFmpeg/FFprobe is required for video/audio metadata extraction
# This is installed via system package manager (apt, yum, etc.)
# For Docker: apt-get install ffmpeg 